App(
    appid="flipper_mcp",
    name="Flipper MCP",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="flipper_mcp_app",
    requires=["gui", "storage", "notification", "expansion"],
    sources=["flipper_mcp.c"],
    fap_category="Tools",
    fap_version=(2, 0),
    fap_description="Manage the Flipper MCP WiFi board — direct UART communication with ESP32.",
)
